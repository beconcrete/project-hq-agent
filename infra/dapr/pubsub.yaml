apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: hq-pubsub
spec:
  # Connection string is injected at container startup by entrypoint.sh.
  type: pubsub.azure.storagequeues
  version: v1
  metadata:
    - name: connectionString
      value: "PLACEHOLDER_REPLACED_AT_RUNTIME"
